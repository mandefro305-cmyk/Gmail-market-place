services:
  - type: web
    name: telegram-gmail-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    disk:
      name: bot-data
      mountPath: /var/data
      sizeGB: 1
